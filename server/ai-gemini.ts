/**
 * Module centralisé pour les appels à l'API Google Gemini.
 * Remplace OpenAI pour la génération IA (risques, regroupement, suggestions).
 */

// Ancien module Gemini. Conservé vide pour éviter les imports cassés
// si quelqu'un a encore une branche ou un build qui le référence.
// L'application utilise désormais `server/ai-ollama.ts`.
export {};
